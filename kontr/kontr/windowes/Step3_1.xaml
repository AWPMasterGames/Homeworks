<Window x:Class="kontr.windowes.Step3_1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kontr.windowes"
        mc:Ignorable="d"
        Title="Step3_1" Height="300" Width="800"
        Closing="Window_Closing">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Backgrounds.xaml"/>
                <ResourceDictionary Source="/Styles/TextBlockStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="{StaticResource Gradient2}" Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1.2*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="ОБРАЗОВАНИЕ" Style="{StaticResource Title}" Grid.ColumnSpan="2"/>
        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="Специализация:" Style="{StaticResource Title}" Grid.Row="0" FontSize="20" Margin="5,5,5,5" Grid.ColumnSpan="2"/>
            <TextBlock Text="Факультет:" Style="{StaticResource Title}" Grid.Row="1" FontSize="20" Margin="5,5,5,5" Grid.ColumnSpan="2"/>

            <TextBox Grid.Row="0" Text="{Binding Specialization}" Grid.Column="2" FontSize="20" Margin="5,5,5,5"/>
            <TextBox Grid.Row="1" Text="{Binding Faculty}" Grid.Column="2" FontSize="20" Margin="5,5,5,5"/>

        </Grid>
        <Button Grid.Row="3" Width="100" Height="25" Content="Вернуться" Click="Button_Click"/>
        <Button Grid.Row="3" Grid.Column="1" Width="100" Height="25" Content="Далее" Click="Button_Click_2"/>
    </Grid>
</Window>
